fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
# Slice 4: reading-progress store on PostgreSQL (sync SQLAlchemy + psycopg 3).
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
# llm-comprehension ticket 11: POST /comprehend calls Claude via the official SDK.
anthropic==0.120.2
# comprehension-response-ux ticket 16: the worker reads pages from the library
# itself, so downscaling moved off the client and needs an image library here.
Pillow==11.0.0
# The schema's owner: migrations replace `create_all`, which can add a table
# but never a column (see docs/manual-migrations.md for what that used to cost).
alembic==1.14.0
